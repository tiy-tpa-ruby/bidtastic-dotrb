  <h1>Welcome to Bidtastic!</h1>

  <h2>Final Tally Report:</h2>
  <p></p>

  <div class="datagrid">
    <table>
      <tbody>
        <thead>
          <tr>
            <th>Image</th>
            <th>Name</th>
            <th>Descrition</th>
            <th>Bid Winner</th>
            <th>Item Price</th>
          </tr>
        </thead>


      <%- @items.each.with_index do |item, index| %>
        <tr class="<%= index.even? ? '' : 'alt' %>">
          <td><%= image_tag(item.image_url(:thumbnail)) %></td>
          <td><%= item.name %></td>
          <td><%= truncate(item.description, length: 26, seperator: ' ') %></td>
          <td><%= item.bid_winner %></td>
          <td><%= item.largest_bid_amount %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
